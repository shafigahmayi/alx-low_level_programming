#include "main.h"
#include <stdio.h>

/**
 * _strlen - measure the lenght fof a string or an array
 * @s : string to be tested
 * Return: void
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
	}
	return (i);
}
